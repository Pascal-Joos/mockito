In the method below, the expression MockUtil.getMockSettings(mockitoMock).getTypeToMock() is not nullable at the point it is used. ```java public Object writeReplace(Object mockitoMock) throws ObjectStreamException { // reentrant lock for critical section. could it be improved ? mutex.lock(); try { // mark started flag // per thread, not per instance // temporary loosy hack to avoid stackoverflow if (mockIsCurrentlyBeingReplaced()) { return mockitoMock; } mockReplacementStarted(); return new CrossClassLoaderSerializationProxy(mockitoMock); } catch (IOException ioe) { MockName mockName = MockUtil.getMockName(mockitoMock); String mockedType = MockUtil.getMockSettings(mockitoMock).getTypeToMock().getCanonicalName(); throw new MockitoSerializationIssue( join( "The mock '" + mockName + "' of type '" + mockedType + "'", "The Java Standard Serialization reported an '" + ioe.getClass().getSimpleName() + "' saying :", " " + ioe.getMessage()), ioe); } finally { // unmark mockReplacementCompleted(); mutex.unlock(); } } ``` Please rewrite the method so that at the line " String mockedType = MockUtil.getMockSettings(mockitoMock).getTypeToMock().getCanonicalName();", at the occurrence of: "MockUtil.getMockSettings(mockitoMock).getTypeToMock()" is replaced with `Nullability.castToNonnull(MockUtil.getMockSettings(mockitoMock).getTypeToMock())`. **Important**: If the expression "MockUtil.getMockSettings(mockitoMock).getTypeToMock()" is referenced further times after the targeted line to rewrite: Everywhere, where non-null is expected for the expression "MockUtil.getMockSettings(mockitoMock).getTypeToMock()" (e.g., at dereference sites), cast the expression to non-null, to prevent follow-on NullAway errors. However, if you would have to add many casts, prefer casting at definition time or reassigning the expression to a local variable and casting there, if this is possible without breaking the code. **Example** - Target non-nullable expression `foo`: ```java public void test(){ String l = this.foo.getBar(); String l2 = this.foo.getFoo(); // this.foo is used again } ``` will be: ```java public void test(){ String l = Nullability.castToNonnull(this.foo).getBar(); String l2 = Nullability.castToNonnull(this.foo).getFoo(); // also added a cast here } ``` ### Requirements: - Do not import any additional libraries or classes - Do not add Java comments, but also do not remove any preexisting comments. - Only modify relevant lines as required - Do not address any other issues - Return only the modified method wrapped in the XML format shown below. Please only provide the fixed code snippet in XML format. I just need the XML response, no other information is needed. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within a ```java ... ``` block. Your response should only be the xml format below, no additional text. ### Response Format: **Provide a meaningful fix:** <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here, JUST THE METHOD. ``` ]]> </code> </response>