In the method below, the expression getModule is not nullable at the point it is used. ```java private void assureCanReadMockito(Set<Class<?>> types) { Method localCanRead = canRead; if (redefineModule == null || getModule == null || localCanRead == null) { return; } Set<Object> modules = new HashSet<Object>(); try { Object target = getModule.invoke( Class.forName( "org.mockito.internal.creation.bytebuddy.inject.MockMethodDispatcher", false, null)); for (Class<?> type : types) { Object module = getModule.invoke(type); if (!modules.contains(module) && !(Boolean) localCanRead.invoke(module, target)) { modules.add(module); } } for (Object module : modules) { redefineModule.invoke( instrumentation, module, Collections.singleton(target), Collections.emptyMap(), Collections.emptyMap(), Collections.emptySet(), Collections.emptyMap()); } } catch (Exception e) { throw new IllegalStateException( join( "Could not adjust module graph to make the mock instance dispatcher visible to some classes", "", "At least one of those modules: " + modules + " is not reading the unnamed module of the bootstrap loader", "Without such a read edge, the classes that are redefined to become mocks cannot access the mock dispatcher.", "To circumvent this, Mockito attempted to add a read edge to this module what failed for an unexpected reason"), e); } } ``` Please rewrite the method so that at the line " getModule.invoke(", at the occurrence of: "getModule" is replaced with `Nullability.castToNonnull(getModule)`. **Important**: If the expression "getModule" is referenced further times after the targeted line to rewrite: Everywhere, where non-null is expected for the expression "getModule" (e.g., at dereference sites), cast the expression to non-null, to prevent follow-on NullAway errors. However, if you would have to add many casts, prefer casting at definition time or reassigning the expression to a local variable and casting there, if this is possible without breaking the code. **Example** - Target non-nullable expression `foo`: ```java public void test(){ String l = this.foo.getBar(); String l2 = this.foo.getFoo(); // this.foo is used again } ``` will be: ```java public void test(){ String l = Nullability.castToNonnull(this.foo).getBar(); String l2 = Nullability.castToNonnull(this.foo).getFoo(); // also added a cast here } ``` ### Requirements: - Do not import any additional libraries or classes - Do not add Java comments, but also do not remove any preexisting comments. - Only modify relevant lines as required - Do not address any other issues - Return only the modified method wrapped in the XML format shown below. Please only provide the fixed code snippet in XML format. I just need the XML response, no other information is needed. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within a ```java ... ``` block. Your response should only be the xml format below, no additional text. ### Response Format: **Provide a meaningful fix:** <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here, JUST THE METHOD. ``` ]]> </code> </response>